import { FormCheckerDirective } from './form-checker.directive';

describe('FormCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new FormCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
